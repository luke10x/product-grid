import React from 'react';
import Layout from '../components/Layout';
import Grid from '../components/Grid';

const Home: React.FC = () => {
  return (
    <Layout>
      <Grid />
    </Layout>
  );
};

export default Home;
